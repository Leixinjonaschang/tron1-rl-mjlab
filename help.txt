usage: scripts/rsl_rl/train.py Mjlab-WF-Tron [-h] [OPTIONS]

╭─ options ────────────────────────────────────────────────────────────────────╮
│ -h, --help                                                                   │
│       show this help message and exit                                        │
│ --registry-name {None}|STR                                                   │
│       (default: None)                                                        │
│ --video {True,False}                                                         │
│       (default: False)                                                       │
│ --video-length INT                                                           │
│       (default: 200)                                                         │
│ --video-interval INT                                                         │
│       (default: 2000)                                                        │
│ --enable-nan-guard {True,False}                                              │
│       (default: False)                                                       │
│ --torchrunx-log-dir {None}|STR                                               │
│       (default: None)                                                        │
│ --wandb-run-path {None}|STR                                                  │
│       (default: None)                                                        │
│ --wandb-checkpoint-name {None}|STR                                           │
│       Optional checkpoint name within the W&B run to load (e.g.              │
│       'model_4000.pt'). (default: None)                                      │
│ --gpu-ids {None}|[INT,...]|{all}                                             │
│       (default: '[0]')                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env options ────────────────────────────────────────────────────────────────╮
│ --env.decimation INT                                                         │
│       Number of physics simulation steps per environment step. Higher values │
│       mean                                                                   │
│       coarser control frequency. Environment step duration = physics_dt *    │
│       decimation. (default: 4)                                               │
│ --env.seed {None}|INT                                                        │
│       Random seed for reproducibility. If None, a random seed is used. The   │
│       actual                                                                 │
│       seed used is stored back into this field after initialization.         │
│       (default: 0)                                                           │
│ --env.episode-length-s FLOAT                                                 │
│       Duration of an episode (in seconds).                                   │
│                                                                              │
│                                                                              │
│       Episode length in steps is computed as:                                │
│         ceil(episode_length_s / (sim.mujoco.timestep * decimation))          │
│       (default: 20.0)                                                        │
│ --env.is-finite-horizon {True,False}                                         │
│       Whether the task has a finite or infinite horizon. Defaults to False   │
│       (infinite).                                                            │
│                                                                              │
│                                                                              │
│       - **Finite horizon (True)**: The time limit defines the task boundary. │
│       When reached,                                                          │
│         no future value exists beyond it, so the agent receives a terminal   │
│       done signal.                                                           │
│       - **Infinite horizon (False)**: The time limit is an artificial        │
│       cutoff. The agent                                                      │
│         receives a truncated done signal to bootstrap the value of           │
│       continuing beyond the                                                  │
│         limit. (default: False)                                              │
│ --env.scale-rewards-by-dt {True,False}                                       │
│       Whether to multiply rewards by the environment step duration (dt).     │
│                                                                              │
│                                                                              │
│       When True (default), reward values are scaled by step_dt to normalize  │
│       cumulative                                                             │
│       episodic rewards across different simulation frequencies. Set to False │
│       for                                                                    │
│       algorithms that expect unscaled reward signals (e.g., HER, static      │
│       reward scaling). (default: True)                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene options ──────────────────────────────────────────────────────────╮
│ Scene configuration defining terrain, entities, and sensors. The scene       │
│ specifies ``num_envs``, the number of parallel environments.                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.num-envs INT                                                     │
│       Number of parallel environments. (default: 4096)                       │
│ --env.scene.env-spacing FLOAT                                                │
│       Spacing between environment origins in meters. (default: 2.0)          │
│ --env.scene.extent {None}|FLOAT                                              │
│       Override for ``mjModel.stat.extent``. If ``None``, MuJoCo computes     │
│       it automatically. (default: 1.0)                                       │
│ --env.scene.spec-fn {fixed}                                                  │
│       Optional callback to modify the ``MjSpec`` after entities and sensors  │
│       have been added but before compilation. (fixed to: None)               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain options ──────────────────────────────────────────────────╮
│ Configuration for terrain as an entity.                                      │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.terrain.spec-fn {fixed}                                          │
│       (fixed to: <function EntityCfg.<lambda>.<locals>.<lambda> at           │
│       0x72adbe5c0cc0>)                                                       │
│ --env.scene.terrain.sort-actuators {True,False}                              │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
│ --env.scene.terrain.terrain-type {generator,plane}                           │
│       Type of terrain to generate. "generator" uses procedural terrain with  │
│       sub-terrain grid, "plane" creates a flat ground plane. (default:       │
│       plane)                                                                 │
│ --env.scene.terrain.env-spacing {None}|FLOAT                                 │
│       Distance between environment origins when using grid layout. Required  │
│       for                                                                    │
│       "plane" terrain or when no sub-terrain origins exist. (default: 2.0)   │
│ --env.scene.terrain.max-init-terrain-level {None}|INT                        │
│       Maximum initial difficulty level (row index) for environment placement │
│       in                                                                     │
│       curriculum mode. None uses all available rows. (default: None)         │
│ --env.scene.terrain.num-envs INT                                             │
│       Number of parallel environments to create. This will get overridden by │
│       the                                                                    │
│       scene configuration if specified there. (default: 1)                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.init-state options ───────────────────────────────────────╮
│ --env.scene.terrain.init-state.pos (FLOAT,FLOAT,FLOAT)                       │
│       Root position and orientation. (default: '(0.0, 0.0, 0.0)')            │
│ --env.scene.terrain.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)                 │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.terrain.init-state.lin-vel (FLOAT,FLOAT,FLOAT)                   │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.terrain.init-state.ang-vel (FLOAT,FLOAT,FLOAT)                   │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.terrain.init-state.joint-pos {None,STR:FLOAT,...}                │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.init-state.joint-vel options ─────────────────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.terrain.init-state.joint-vel.* FLOAT                             │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.lights.0 options ─────────────────────────────────────────╮
│ --env.scene.terrain.lights.0.name {None}|STR                                 │
│       Name of the light (optional). (default: sun)                           │
│ --env.scene.terrain.lights.0.body STR                                        │
│       Body to attach light to (default: "world"). (default: world)           │
│ --env.scene.terrain.lights.0.mode STR                                        │
│       Light mode ("fixed", "track", "trackcom", "targetbody",                │
│       "targetbodycom"). (default: fixed)                                     │
│ --env.scene.terrain.lights.0.target {None}|STR                               │
│       Target body for tracking modes (optional). (default: None)             │
│ --env.scene.terrain.lights.0.type {spot,directional}                         │
│       Light type ("spot" or "directional"). (default: directional)           │
│ --env.scene.terrain.lights.0.castshadow {True,False}                         │
│       Whether light casts shadows. (default: True)                           │
│ --env.scene.terrain.lights.0.pos (FLOAT,FLOAT,FLOAT)                         │
│       Light position (x, y, z). (default: '(0.0, 0.0, 1.5)')                 │
│ --env.scene.terrain.lights.0.dir (FLOAT,FLOAT,FLOAT)                         │
│       Light direction vector (x, y, z). (default: '(0.0, 0.0, -1.0)')        │
│ --env.scene.terrain.lights.0.cutoff FLOAT                                    │
│       Spot light cutoff angle in degrees. (default: 45.0)                    │
│ --env.scene.terrain.lights.0.exponent FLOAT                                  │
│       Spot light exponent. (default: 10.0)                                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.textures.0 options ───────────────────────────────────────╮
│ --env.scene.terrain.textures.0.name STR                                      │
│       Name of the texture. (default: groundplane)                            │
│ --env.scene.terrain.textures.0.type {2d,cube,skybox}                         │
│       Texture type ("2d", "cube", or "skybox"). (default: 2d)                │
│ --env.scene.terrain.textures.0.builtin {checker,gradient,flat,none}          │
│       Built-in texture pattern ("checker", "gradient", "flat", or "none").   │
│       (default: checker)                                                     │
│ --env.scene.terrain.textures.0.rgb1 (FLOAT,FLOAT,FLOAT)                      │
│       First RGB color tuple. (default: '(0.2, 0.3, 0.4)')                    │
│ --env.scene.terrain.textures.0.rgb2 (FLOAT,FLOAT,FLOAT)                      │
│       Second RGB color tuple. (default: '(0.1, 0.2, 0.3)')                   │
│ --env.scene.terrain.textures.0.width INT                                     │
│       Texture width in pixels (must be positive). (default: 300)             │
│ --env.scene.terrain.textures.0.height INT                                    │
│       Texture height in pixels (must be positive). (default: 300)            │
│ --env.scene.terrain.textures.0.mark {edge,cross,random,none}                 │
│       Marking pattern ("edge", "cross", "random", or "none"). (default:      │
│       edge)                                                                  │
│ --env.scene.terrain.textures.0.markrgb (FLOAT,FLOAT,FLOAT)                   │
│       RGB color for markings. (default: '(0.8, 0.8, 0.8)')                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.terrain.materials.0 options ──────────────────────────────────────╮
│ --env.scene.terrain.materials.0.name STR                                     │
│       Name of the material. (default: groundplane)                           │
│ --env.scene.terrain.materials.0.rgba (FLOAT,FLOAT,FLOAT,FLOAT)               │
│       RGBA color of the material. Without a texture this is the direct       │
│       surface color; with a texture it multiplies the texture colors.        │
│       (default: '(1.0, 1.0, 1.0, 1.0)')                                      │
│ --env.scene.terrain.materials.0.texuniform {True,False}                      │
│       Whether texture coordinates are uniform. (default: True)               │
│ --env.scene.terrain.materials.0.texrepeat (FLOAT,FLOAT)                      │
│       Texture repeat pattern (width, height). Must be positive. (default:    │
│       '(4.0, 4.0)')                                                          │
│ --env.scene.terrain.materials.0.reflectance FLOAT                            │
│       Material reflectance value. (default: 0.2)                             │
│ --env.scene.terrain.materials.0.texture {None}|STR                           │
│       Name of texture to apply (optional). (default: groundplane)            │
│ --env.scene.terrain.materials.0.geom-names-expr {None}|(STR,...)             │
│       Regex patterns to match geom names. Matching geoms will have their     │
│       material set to this material. ``None`` means no assignment. (default: │
│       '('"'"'terrain$'"'"',)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot options ───────────────────────────────────────────╮
│ --env.scene.entities.robot.spec-fn {fixed}                                   │
│       (fixed to: <function get_spec at 0x72adbe5c0ae0>)                      │
│ --env.scene.entities.robot.sort-actuators {True,False}                       │
│       When True, reorder actuators so that ``model.ctrl`` follows            │
│       joint/tendon/site                                                      │
│       definition order rather than the order actuators appear in the config. │
│       XML actuators                                                          │
│       are excluded from sorting and always retain their declaration order.   │
│       (default: False)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.init-state options ────────────────────────────────╮
│ --env.scene.entities.robot.init-state.pos (FLOAT,FLOAT,FLOAT)                │
│       Root position and orientation. (default: '(0.0, 0.0,                   │
│       0.9660000000000001)')                                                  │
│ --env.scene.entities.robot.init-state.rot (FLOAT,FLOAT,FLOAT,FLOAT)          │
│       Root position and orientation. (default: '(1.0, 0.0, 0.0, 0.0)')       │
│ --env.scene.entities.robot.init-state.lin-vel (FLOAT,FLOAT,FLOAT)            │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.robot.init-state.ang-vel (FLOAT,FLOAT,FLOAT)            │
│       Root linear and angular velocity (only for floating base entities).    │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.scene.entities.robot.init-state.joint-pos {None,STR:FLOAT,...}         │
│       Articulation (only for articulated entities). Set to None to use the   │
│       model's existing keyframe (errors if none exists). (default:           │
│       '{'"'"'.*'"'"': 0.0}')                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.init-state.joint-vel options ──────────────────────╮
│ Articulation (only for articulated entities). Set to None to use the model's │
│ existing keyframe (errors if none exists).                                   │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.scene.entities.robot.init-state.joint-vel.* FLOAT                      │
│       (default: 0.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.articulation options ──────────────────────────────╮
│ --env.scene.entities.robot.articulation.soft-joint-pos-limit-factor FLOAT    │
│       (default: 1.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.articulation.actuators.0 options ──────────────────╮
│ --env.scene.entities.robot.articulation.actuators.0.target-names-expr (STR,  │
│ ...)                                                                         │
│       Targets that are part of this actuator group.                          │
│                                                                              │
│                                                                              │
│       Can be a tuple of names or tuple of regex expressions.                 │
│       Interpreted based on transmission_type (joint/tendon/site). (default:  │
│       '('"'"'abad_[RL]_Joint'"'"', '"'"'hip_[RL]_Joint'"'"',                 │
│       '"'"'knee_[RL]_Joint'"'"')')                                           │
│ --env.scene.entities.robot.articulation.actuators.0.transmission-type        │
│ {JOINT,TENDON,SITE}                                                          │
│       Transmission type. Defaults to JOINT. (default: JOINT)                 │
│ --env.scene.entities.robot.articulation.actuators.0.armature FLOAT           │
│       Reflected rotor inertia. (default: 0.0)                                │
│ --env.scene.entities.robot.articulation.actuators.0.frictionloss FLOAT       │
│       Friction loss force limit.                                             │
│                                                                              │
│                                                                              │
│       Applies a constant friction force opposing motion, independent of load │
│       or velocity.                                                           │
│       Also known as dry friction or load-independent friction. (default:     │
│       0.0)                                                                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.scene.entities.robot.articulation.actuators.1 options ──────────────────╮
│ --env.scene.entities.robot.articulation.actuators.1.target-names-expr (STR,  │
│ ...)                                                                         │
│       Targets that are part of this actuator group.                          │
│                                                                              │
│                                                                              │
│       Can be a tuple of names or tuple of regex expressions.                 │
│       Interpreted based on transmission_type (joint/tendon/site). (default:  │
│       '('"'"'wheel_[RL]_Joint'"'"',)')                                       │
│ --env.scene.entities.robot.articulation.actuators.1.transmission-type        │
│ {JOINT,TENDON,SITE}                                                          │
│       Transmission type. Defaults to JOINT. (default: JOINT)                 │
│ --env.scene.entities.robot.articulation.actuators.1.armature FLOAT           │
│       Reflected rotor inertia. (default: 0.0)                                │
│ --env.scene.entities.robot.articulation.actuators.1.frictionloss FLOAT       │
│       Friction loss force limit.                                             │
│                                                                              │
│                                                                              │
│       Applies a constant friction force opposing motion, independent of load │
│       or velocity.                                                           │
│       Also known as dry friction or load-independent friction. (default:     │
│       0.0)                                                                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor options ─────────────────────────────────────────────╮
│ Configuration for an observation group. An observation group bundles         │
│ multiple observation terms together. Groups are typically used to separate   │
│ observations for different purposes (e.g., "actor" for the actor, "critic"   │
│ for the value function).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.concatenate-terms {True,False}                      │
│       Whether to concatenate all terms into a single tensor. If False,       │
│       returns                                                                │
│       a dict mapping term names to their individual tensors. (default: True) │
│ --env.observations.actor.concatenate-dim INT                                 │
│       Dimension along which to concatenate terms. Default -1 (last           │
│       dimension). (default: -1)                                              │
│ --env.observations.actor.enable-corruption {True,False}                      │
│       Whether to apply noise corruption to observations. Set to True during  │
│       training for domain randomization, False during evaluation. (default:  │
│       True)                                                                  │
│ --env.observations.actor.history-length {None}|INT                           │
│       Group-level history length override. If set, applies to all terms in   │
│       this group. If None, each term uses its own ``history_length``         │
│       setting. (default: 20)                                                 │
│ --env.observations.actor.flatten-history-dim {True,False}                    │
│       Whether to flatten history into the observation dimension. If True,    │
│       observations have shape ``(num_envs, obs_dim * history_length)``. If   │
│       False,                                                                 │
│       shape is ``(num_envs, history_length, obs_dim)``. (default: True)      │
│ --env.observations.actor.nan-policy {disabled,warn,sanitize,error}           │
│       NaN/Inf handling policy for observations in this group.                │
│                                                                              │
│                                                                              │
│       - 'disabled': No checks (default, fastest)                             │
│       - 'warn': Log warning with term name and env IDs, then sanitize        │
│       (debugging)                                                            │
│       - 'sanitize': Silent sanitization to 0.0 like reward manager (safe for │
│       production)                                                            │
│       - 'error': Raise ValueError on NaN/Inf (strict development mode)       │
│       (default: disabled)                                                    │
│ --env.observations.actor.nan-check-per-term {True,False}                     │
│       If True, check each observation term individually to identify NaN      │
│       source.                                                                │
│       If False, check only the final concatenated output (faster but less    │
│       informative).                                                          │
│       Only applies when nan_policy != 'disabled'. (default: True)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.base-pose-commands options ────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.base-pose-commands.func {fixed}               │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_commands_b at 0x72adbe5c16c0>)                      │
│ --env.observations.actor.terms.base-pose-commands.clip {None}|(FLOAT,FLOAT)  │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.base-pose-commands.scale {fixed}              │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.base-pose-commands.delay-min-lag INT          │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-pose-commands.delay-max-lag INT          │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.base-pose-commands.delay-per-env {True,False} │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.base-pose-commands.delay-hold-prob FLOAT      │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.base-pose-commands.delay-update-period INT    │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.base-pose-commands.delay-per-env-phase {True, │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.base-pose-commands.history-length INT         │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-pose-commands.flatten-history-dim {True, │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.base-se3-decrease-rate options ────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.base-se3-decrease-rate.func {fixed}           │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_se3_decrease_rate at 0x72adbe5c1800>)               │
│ --env.observations.actor.terms.base-se3-decrease-rate.clip {None}|(FLOAT,    │
│ FLOAT)                                                                       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.base-se3-decrease-rate.scale {fixed}          │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-min-lag INT      │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-max-lag INT      │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-per-env {True,   │
│ False}                                                                       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-hold-prob FLOAT  │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-update-period    │
│ INT                                                                          │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.base-se3-decrease-rate.delay-per-env-phase    │
│ {True,False}                                                                 │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.base-se3-decrease-rate.history-length INT     │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-se3-decrease-rate.flatten-history-dim    │
│ {True,False}                                                                 │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.base-commands-vel options ─────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.base-commands-vel.func {fixed}                │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_commands_vel_c at 0x72adbe5c18a0>)                  │
│ --env.observations.actor.terms.base-commands-vel.clip {None}|(FLOAT,FLOAT)   │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.base-commands-vel.scale {fixed}               │
│       Scaling factor(s) to multiply the observation by. (fixed to: None)     │
│ --env.observations.actor.terms.base-commands-vel.delay-min-lag INT           │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-commands-vel.delay-max-lag INT           │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.base-commands-vel.delay-per-env {True,False}  │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.base-commands-vel.delay-hold-prob FLOAT       │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.base-commands-vel.delay-update-period INT     │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.base-commands-vel.delay-per-env-phase {True,  │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.base-commands-vel.history-length INT          │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-commands-vel.flatten-history-dim {True,  │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.base-ang-vel options ──────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.base-ang-vel.func {fixed}                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_ang_vel at 0x72adc3d8ea20>)                         │
│ --env.observations.actor.terms.base-ang-vel.clip {None}|(FLOAT,FLOAT)        │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.base-ang-vel.scale {fixed}                    │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.25)     │
│ --env.observations.actor.terms.base-ang-vel.delay-min-lag INT                │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-ang-vel.delay-max-lag INT                │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.base-ang-vel.delay-per-env {True,False}       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.base-ang-vel.delay-hold-prob FLOAT            │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.base-ang-vel.delay-update-period INT          │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.base-ang-vel.delay-per-env-phase {True,False} │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.base-ang-vel.history-length INT               │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.base-ang-vel.flatten-history-dim {True,False} │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.base-ang-vel.noise options ────────────────────╮
│ --env.observations.actor.terms.base-ang-vel.noise.operation {add,scale,abs}  │
│       (default: add)                                                         │
│ --env.observations.actor.terms.base-ang-vel.noise.mean FLOAT|(FLOAT,...)     │
│       (default: 0.0)                                                         │
│ --env.observations.actor.terms.base-ang-vel.noise.std FLOAT|(FLOAT,...)      │
│       (default: 0.05)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.proj-gravity options ──────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.proj-gravity.func {fixed}                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function projected_gravity at 0x72adc3d8eac0>)                    │
│ --env.observations.actor.terms.proj-gravity.clip {None}|(FLOAT,FLOAT)        │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.proj-gravity.scale {fixed}                    │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.actor.terms.proj-gravity.delay-min-lag INT                │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.proj-gravity.delay-max-lag INT                │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.proj-gravity.delay-per-env {True,False}       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.proj-gravity.delay-hold-prob FLOAT            │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.proj-gravity.delay-update-period INT          │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.proj-gravity.delay-per-env-phase {True,False} │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.proj-gravity.history-length INT               │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.proj-gravity.flatten-history-dim {True,False} │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.proj-gravity.noise options ────────────────────╮
│ --env.observations.actor.terms.proj-gravity.noise.operation {add,scale,abs}  │
│       (default: add)                                                         │
│ --env.observations.actor.terms.proj-gravity.noise.mean FLOAT|(FLOAT,...)     │
│       (default: 0.0)                                                         │
│ --env.observations.actor.terms.proj-gravity.noise.std FLOAT|(FLOAT,...)      │
│       (default: 0.025)                                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.joint-pos options ─────────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.joint-pos.func {fixed}                        │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_pos_rel at 0x72adc3d8eb60>)                        │
│ --env.observations.actor.terms.joint-pos.clip {None}|(FLOAT,FLOAT)           │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.joint-pos.scale {fixed}                       │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.actor.terms.joint-pos.delay-min-lag INT                   │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.joint-pos.delay-max-lag INT                   │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.joint-pos.delay-per-env {True,False}          │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.joint-pos.delay-hold-prob FLOAT               │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.joint-pos.delay-update-period INT             │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.joint-pos.delay-per-env-phase {True,False}    │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.joint-pos.history-length INT                  │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.joint-pos.flatten-history-dim {True,False}    │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.joint-pos.params.asset-cfg options ────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.name STR           │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.joint-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '('"'"'abad_[RL]_Joint'"'"', '"'"'hip_[RL]_Joint'"'"',                 │
│       '"'"'knee_[RL]_Joint'"'"')')                                           │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.joint-ids {fixed}  │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.body-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.body-ids {fixed}   │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.geom-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.geom-ids {fixed}   │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.site-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.site-ids {fixed}   │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.actuator-names     │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.actuator-ids       │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.tendon-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.tendon-ids {fixed} │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.camera-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.camera-ids {fixed} │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.light-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.light-ids {fixed}  │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.material-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.material-ids       │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.actor.terms.joint-pos.params.asset-cfg.preserve-order     │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.joint-pos.noise options ───────────────────────╮
│ --env.observations.actor.terms.joint-pos.noise.operation {add,scale,abs}     │
│       (default: add)                                                         │
│ --env.observations.actor.terms.joint-pos.noise.mean FLOAT|(FLOAT,...)        │
│       (default: 0.0)                                                         │
│ --env.observations.actor.terms.joint-pos.noise.std FLOAT|(FLOAT,...)         │
│       (default: 0.01)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.joint-vel options ─────────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.joint-vel.func {fixed}                        │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_vel_rel at 0x72adc3d8ec00>)                        │
│ --env.observations.actor.terms.joint-vel.clip {None}|(FLOAT,FLOAT)           │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.joint-vel.scale {fixed}                       │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.05)     │
│ --env.observations.actor.terms.joint-vel.delay-min-lag INT                   │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.joint-vel.delay-max-lag INT                   │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.joint-vel.delay-per-env {True,False}          │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.joint-vel.delay-hold-prob FLOAT               │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.joint-vel.delay-update-period INT             │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.joint-vel.delay-per-env-phase {True,False}    │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.joint-vel.history-length INT                  │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.joint-vel.flatten-history-dim {True,False}    │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.joint-vel.noise options ───────────────────────╮
│ --env.observations.actor.terms.joint-vel.noise.operation {add,scale,abs}     │
│       (default: add)                                                         │
│ --env.observations.actor.terms.joint-vel.noise.mean FLOAT|(FLOAT,...)        │
│       (default: 0.0)                                                         │
│ --env.observations.actor.terms.joint-vel.noise.std FLOAT|(FLOAT,...)         │
│       (default: 0.01)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.last-action options ───────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.actor.terms.last-action.func {fixed}                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function last_action at 0x72adc3d8eca0>)                          │
│ --env.observations.actor.terms.last-action.clip {None}|(FLOAT,FLOAT)         │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.actor.terms.last-action.scale {fixed}                     │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.actor.terms.last-action.delay-min-lag INT                 │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.last-action.delay-max-lag INT                 │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.actor.terms.last-action.delay-per-env {True,False}        │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.actor.terms.last-action.delay-hold-prob FLOAT             │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.actor.terms.last-action.delay-update-period INT           │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.actor.terms.last-action.delay-per-env-phase {True,False}  │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.actor.terms.last-action.history-length INT                │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.actor.terms.last-action.flatten-history-dim {True,False}  │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.actor.terms.last-action.noise options ─────────────────────╮
│ --env.observations.actor.terms.last-action.noise.operation {add,scale,abs}   │
│       (default: add)                                                         │
│ --env.observations.actor.terms.last-action.noise.mean FLOAT|(FLOAT,...)      │
│       (default: 0.0)                                                         │
│ --env.observations.actor.terms.last-action.noise.std FLOAT|(FLOAT,...)       │
│       (default: 0.01)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic options ────────────────────────────────────────────╮
│ Configuration for an observation group. An observation group bundles         │
│ multiple observation terms together. Groups are typically used to separate   │
│ observations for different purposes (e.g., "actor" for the actor, "critic"   │
│ for the value function).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.concatenate-terms {True,False}                     │
│       Whether to concatenate all terms into a single tensor. If False,       │
│       returns                                                                │
│       a dict mapping term names to their individual tensors. (default: True) │
│ --env.observations.critic.concatenate-dim INT                                │
│       Dimension along which to concatenate terms. Default -1 (last           │
│       dimension). (default: -1)                                              │
│ --env.observations.critic.enable-corruption {True,False}                     │
│       Whether to apply noise corruption to observations. Set to True during  │
│       training for domain randomization, False during evaluation. (default:  │
│       False)                                                                 │
│ --env.observations.critic.history-length {None}|INT                          │
│       Group-level history length override. If set, applies to all terms in   │
│       this group. If None, each term uses its own ``history_length``         │
│       setting. (default: None)                                               │
│ --env.observations.critic.flatten-history-dim {True,False}                   │
│       Whether to flatten history into the observation dimension. If True,    │
│       observations have shape ``(num_envs, obs_dim * history_length)``. If   │
│       False,                                                                 │
│       shape is ``(num_envs, history_length, obs_dim)``. (default: True)      │
│ --env.observations.critic.nan-policy {disabled,warn,sanitize,error}          │
│       NaN/Inf handling policy for observations in this group.                │
│                                                                              │
│                                                                              │
│       - 'disabled': No checks (default, fastest)                             │
│       - 'warn': Log warning with term name and env IDs, then sanitize        │
│       (debugging)                                                            │
│       - 'sanitize': Silent sanitization to 0.0 like reward manager (safe for │
│       production)                                                            │
│       - 'error': Raise ValueError on NaN/Inf (strict development mode)       │
│       (default: disabled)                                                    │
│ --env.observations.critic.nan-check-per-term {True,False}                    │
│       If True, check each observation term individually to identify NaN      │
│       source.                                                                │
│       If False, check only the final concatenated output (faster but less    │
│       informative).                                                          │
│       Only applies when nan_policy != 'disabled'. (default: True)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.base-lin-vel options ─────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.base-lin-vel.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_lin_vel at 0x72adc3d8e980>)                         │
│ --env.observations.critic.terms.base-lin-vel.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.base-lin-vel.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.critic.terms.base-lin-vel.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-lin-vel.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.base-lin-vel.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.base-lin-vel.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.base-lin-vel.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.base-lin-vel.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.base-lin-vel.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-lin-vel.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.base-ang-vel options ─────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.base-ang-vel.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_ang_vel at 0x72adc3d8ea20>)                         │
│ --env.observations.critic.terms.base-ang-vel.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.base-ang-vel.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.25)     │
│ --env.observations.critic.terms.base-ang-vel.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-ang-vel.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.base-ang-vel.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.base-ang-vel.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.base-ang-vel.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.base-ang-vel.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.base-ang-vel.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-ang-vel.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.proj-gravity options ─────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.proj-gravity.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function projected_gravity at 0x72adc3d8eac0>)                    │
│ --env.observations.critic.terms.proj-gravity.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.proj-gravity.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.critic.terms.proj-gravity.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.proj-gravity.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.proj-gravity.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.proj-gravity.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.proj-gravity.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.proj-gravity.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.proj-gravity.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.proj-gravity.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.joint-pos options ────────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.joint-pos.func {fixed}                       │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_pos_rel at 0x72adc3d8eb60>)                        │
│ --env.observations.critic.terms.joint-pos.clip {None}|(FLOAT,FLOAT)          │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.joint-pos.scale {fixed}                      │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.critic.terms.joint-pos.delay-min-lag INT                  │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-pos.delay-max-lag INT                  │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.joint-pos.delay-per-env {True,False}         │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.joint-pos.delay-hold-prob FLOAT              │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.joint-pos.delay-update-period INT            │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.joint-pos.delay-per-env-phase {True,False}   │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.joint-pos.history-length INT                 │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-pos.flatten-history-dim {True,False}   │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.joint-pos.params.asset-cfg options ───────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.name STR          │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.joint-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '('"'"'abad_[RL]_Joint'"'"', '"'"'hip_[RL]_Joint'"'"',                 │
│       '"'"'knee_[RL]_Joint'"'"')')                                           │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.joint-ids {fixed} │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.body-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.body-ids {fixed}  │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.geom-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.geom-ids {fixed}  │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.site-names        │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.site-ids {fixed}  │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.actuator-names    │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.actuator-ids      │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.tendon-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.tendon-ids        │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.camera-names      │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.camera-ids        │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.light-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.light-ids {fixed} │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.material-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.material-ids      │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.joint-pos.params.asset-cfg.preserve-order    │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.joint-vel options ────────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.joint-vel.func {fixed}                       │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_vel_rel at 0x72adc3d8ec00>)                        │
│ --env.observations.critic.terms.joint-vel.clip {None}|(FLOAT,FLOAT)          │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.joint-vel.scale {fixed}                      │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.05)     │
│ --env.observations.critic.terms.joint-vel.delay-min-lag INT                  │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-vel.delay-max-lag INT                  │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.joint-vel.delay-per-env {True,False}         │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.joint-vel.delay-hold-prob FLOAT              │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.joint-vel.delay-update-period INT            │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.joint-vel.delay-per-env-phase {True,False}   │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.joint-vel.history-length INT                 │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-vel.flatten-history-dim {True,False}   │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.last-action options ──────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.last-action.func {fixed}                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function last_action at 0x72adc3d8eca0>)                          │
│ --env.observations.critic.terms.last-action.clip {None}|(FLOAT,FLOAT)        │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.last-action.scale {fixed}                    │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.0)      │
│ --env.observations.critic.terms.last-action.delay-min-lag INT                │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.last-action.delay-max-lag INT                │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.last-action.delay-per-env {True,False}       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.last-action.delay-hold-prob FLOAT            │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.last-action.delay-update-period INT          │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.last-action.delay-per-env-phase {True,False} │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.last-action.history-length INT               │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.last-action.flatten-history-dim {True,False} │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.joint-torque options ─────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.joint-torque.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function actuator_force at 0x72adbe5c13a0>)                       │
│ --env.observations.critic.terms.joint-torque.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.joint-torque.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.01)     │
│ --env.observations.critic.terms.joint-torque.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-torque.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.joint-torque.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.joint-torque.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.joint-torque.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.joint-torque.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.joint-torque.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-torque.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.joint-acc options ────────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.joint-acc.func {fixed}                       │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_acc at 0x72adbe5c1300>)                            │
│ --env.observations.critic.terms.joint-acc.clip {None}|(FLOAT,FLOAT)          │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.joint-acc.scale {fixed}                      │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.1)      │
│ --env.observations.critic.terms.joint-acc.delay-min-lag INT                  │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-acc.delay-max-lag INT                  │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.joint-acc.delay-per-env {True,False}         │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.joint-acc.delay-hold-prob FLOAT              │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.joint-acc.delay-update-period INT            │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.joint-acc.delay-per-env-phase {True,False}   │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.joint-acc.history-length INT                 │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.joint-acc.flatten-history-dim {True,False}   │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.feet-lin-vel options ─────────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.feet-lin-vel.func {fixed}                    │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function body_lin_vel at 0x72adbe5c1440>)                         │
│ --env.observations.critic.terms.feet-lin-vel.clip {None}|(FLOAT,FLOAT)       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.feet-lin-vel.scale {fixed}                   │
│       Scaling factor(s) to multiply the observation by. (fixed to: 0.1)      │
│ --env.observations.critic.terms.feet-lin-vel.delay-min-lag INT               │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.feet-lin-vel.delay-max-lag INT               │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.feet-lin-vel.delay-per-env {True,False}      │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.feet-lin-vel.delay-hold-prob FLOAT           │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.feet-lin-vel.delay-update-period INT         │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.feet-lin-vel.delay-per-env-phase {True,      │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.feet-lin-vel.history-length INT              │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.feet-lin-vel.flatten-history-dim {True,      │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.feet-lin-vel.params.asset-cfg options ────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.name STR       │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.joint-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.joint-ids      │
│ {fixed}                                                                      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.body-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       'wheel_.*')                                                            │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.body-ids       │
│ {fixed}                                                                      │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.geom-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.geom-ids       │
│ {fixed}                                                                      │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.site-names     │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.site-ids       │
│ {fixed}                                                                      │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.actuator-names │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.actuator-ids   │
│ {fixed}                                                                      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.tendon-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.tendon-ids     │
│ {fixed}                                                                      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.camera-names   │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.camera-ids     │
│ {fixed}                                                                      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.light-names    │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.light-ids      │
│ {fixed}                                                                      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.material-names │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.material-ids   │
│ {fixed}                                                                      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.observations.critic.terms.feet-lin-vel.params.asset-cfg.preserve-order │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.base-height-error options ────────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.base-height-error.func {fixed}               │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function base_height_error at 0x72adbe5c14e0>)                    │
│ --env.observations.critic.terms.base-height-error.clip {None}|(FLOAT,FLOAT)  │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.base-height-error.scale {fixed}              │
│       Scaling factor(s) to multiply the observation by. (fixed to: 3.0)      │
│ --env.observations.critic.terms.base-height-error.delay-min-lag INT          │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-height-error.delay-max-lag INT          │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.base-height-error.delay-per-env {True,False} │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.base-height-error.delay-hold-prob FLOAT      │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.base-height-error.delay-update-period INT    │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.base-height-error.delay-per-env-phase {True, │
│ False}                                                                       │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.base-height-error.history-length INT         │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.base-height-error.flatten-history-dim {True, │
│ False}                                                                       │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.observations.critic.terms.foot-rel-position-w options ──────────────────╮
│ Configuration for an observation term. Processing pipeline: compute → noise  │
│ → clip → scale → delay → history. Delay models sensor latency. History       │
│ provides temporal context. Both are optional and can be combined.            │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.observations.critic.terms.foot-rel-position-w.func {fixed}             │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function foot_rel_position_w at 0x72adbe5c1580>)                  │
│ --env.observations.critic.terms.foot-rel-position-w.clip {None}|(FLOAT,      │
│ FLOAT)                                                                       │
│       Range (min, max) to clip the observation values. (default: None)       │
│ --env.observations.critic.terms.foot-rel-position-w.scale {fixed}            │
│       Scaling factor(s) to multiply the observation by. (fixed to: 1.5)      │
│ --env.observations.critic.terms.foot-rel-position-w.delay-min-lag INT        │
│       Minimum lag (in steps) for delayed observations. Lag sampled uniformly │
│       from                                                                   │
│       [min_lag, max_lag]. Convert to ms: lag * (1000 / control_hz).          │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.foot-rel-position-w.delay-max-lag INT        │
│       Maximum lag (in steps) for delayed observations. Use min=max for       │
│       constant delay. (default: 0)                                           │
│ --env.observations.critic.terms.foot-rel-position-w.delay-per-env {True,     │
│ False}                                                                       │
│       If True, each environment samples its own lag. If False, all           │
│       environments share                                                     │
│       the same lag at each step. (default: True)                             │
│ --env.observations.critic.terms.foot-rel-position-w.delay-hold-prob FLOAT    │
│       Probability of reusing the previous lag instead of resampling. Useful  │
│       for                                                                    │
│       temporally correlated latency patterns. (default: 0.0)                 │
│ --env.observations.critic.terms.foot-rel-position-w.delay-update-period INT  │
│       Resample lag every N steps (models multi-rate sensors). If 0, update   │
│       every step. (default: 0)                                               │
│ --env.observations.critic.terms.foot-rel-position-w.delay-per-env-phase      │
│ {True,False}                                                                 │
│       If True and update_period > 0, stagger update timing across envs to    │
│       avoid                                                                  │
│       synchronized resampling. (default: True)                               │
│ --env.observations.critic.terms.foot-rel-position-w.history-length INT       │
│       Number of past observations to keep in history. 0 = no history.        │
│       (default: 0)                                                           │
│ --env.observations.critic.terms.foot-rel-position-w.flatten-history-dim      │
│ {True,False}                                                                 │
│       Whether to flatten the history dimension into observation.             │
│                                                                              │
│                                                                              │
│       When True and concatenate_terms=True, uses term-major ordering:        │
│       [A_t0, A_t1, ..., A_tH-1, B_t0, B_t1, ..., B_tH-1, ...]                │
│       See docs/source/observation.rst for details on ordering. (default:     │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.actions.joint-pos options ──────────────────────────────────────────────╮
│ Configuration for joint position control.                                    │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.actions.joint-pos.entity-name STR                                      │
│       Name of the entity in the scene that this action term controls.        │
│       (default: robot)                                                       │
│ --env.actions.joint-pos.clip {None,STR:TUPLE,...}                            │
│       Optional clipping bounds per transmission type. Maps transmission name │
│       (e.g., 'position', 'velocity') to (min, max) tuple. (default: None)    │
│ --env.actions.joint-pos.transmission-type {JOINT,TENDON,SITE}                │
│       Type of transmission to control. (default: JOINT)                      │
│ --env.actions.joint-pos.actuator-names (STR,...)|[STR,...]                   │
│       Actuator names to control. (default: '('"'"'abad_[RL]_Joint'"'"',      │
│       '"'"'hip_[RL]_Joint'"'"', '"'"'knee_[RL]_Joint'"'"')')                 │
│ --env.actions.joint-pos.scale FLOAT|{STR:FLOAT,...}                          │
│       Action scale. Float or dict mapping actuator names to scales.          │
│       (default: 0.5)                                                         │
│ --env.actions.joint-pos.offset FLOAT|{STR:FLOAT,...}                         │
│       Action offset. Float or dict mapping actuator names to offsets.        │
│       (default: 0.0)                                                         │
│ --env.actions.joint-pos.preserve-order {True,False}                          │
│       Whether to preserve the order of actuator names. (default: False)      │
│ --env.actions.joint-pos.use-default-offset {True,False}                      │
│       (default: True)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.actions.joint-vel options ──────────────────────────────────────────────╮
│ Configuration for joint velocity control.                                    │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.actions.joint-vel.entity-name STR                                      │
│       Name of the entity in the scene that this action term controls.        │
│       (default: robot)                                                       │
│ --env.actions.joint-vel.clip {None,STR:TUPLE,...}                            │
│       Optional clipping bounds per transmission type. Maps transmission name │
│       (e.g., 'position', 'velocity') to (min, max) tuple. (default: None)    │
│ --env.actions.joint-vel.transmission-type {JOINT,TENDON,SITE}                │
│       Type of transmission to control. (default: JOINT)                      │
│ --env.actions.joint-vel.actuator-names (STR,...)|[STR,...]                   │
│       Actuator names to control. (default: '('"'"'wheel_[RL]_Joint'"'"',)')  │
│ --env.actions.joint-vel.scale FLOAT|{STR:FLOAT,...}                          │
│       Action scale. Float or dict mapping actuator names to scales.          │
│       (default: 5.0)                                                         │
│ --env.actions.joint-vel.offset FLOAT|{STR:FLOAT,...}                         │
│       Action offset. Float or dict mapping actuator names to offsets.        │
│       (default: 0.0)                                                         │
│ --env.actions.joint-vel.preserve-order {True,False}                          │
│       Whether to preserve the order of actuator names. (default: False)      │
│ --env.actions.joint-vel.use-default-offset {True,False}                      │
│       (default: True)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.prepare-quantities options ──────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.prepare-quantities.func {fixed}                                 │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function prepare_quantities at 0x72adbe5c1c60>)                   │
│ --env.events.prepare-quantities.mode {startup,reset,interval,step}           │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: startup)                                  │
│ --env.events.prepare-quantities.interval-range-s {None}|(FLOAT,FLOAT)        │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.is-global-time {True,False}                  │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.prepare-quantities.min-step-count-between-reset INT             │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.prepare-quantities.params.asset-cfg options ─────────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.prepare-quantities.params.asset-cfg.name STR                    │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.prepare-quantities.params.asset-cfg.joint-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.prepare-quantities.params.asset-cfg.joint-ids {fixed}           │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.body-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.prepare-quantities.params.asset-cfg.body-ids {fixed}            │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.geom-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.params.asset-cfg.geom-ids {fixed}            │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.site-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.prepare-quantities.params.asset-cfg.site-ids {fixed}            │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.actuator-names              │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.params.asset-cfg.actuator-ids {fixed}        │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.tendon-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.params.asset-cfg.tendon-ids {fixed}          │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.camera-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.params.asset-cfg.camera-ids {fixed}          │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.light-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.prepare-quantities.params.asset-cfg.light-ids {fixed}           │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.material-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.prepare-quantities.params.asset-cfg.material-ids {fixed}        │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.prepare-quantities.params.asset-cfg.preserve-order {True,False} │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-base-mass options ───────────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-base-mass.func {fixed}                                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function body_mass at 0x72adc3d78900>)                            │
│ --env.events.add-base-mass.mode {startup,reset,interval,step}                │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: startup)                                  │
│ --env.events.add-base-mass.interval-range-s {None}|(FLOAT,FLOAT)             │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.add-base-mass.is-global-time {True,False}                       │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.add-base-mass.min-step-count-between-reset INT                  │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-base-mass.params options ────────────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-base-mass.params.ranges (FLOAT,...)                         │
│       (default: '(-2.0, 5.0)')                                               │
│ --env.events.add-base-mass.params.operation STR                              │
│       (default: add)                                                         │
│ --env.events.add-base-mass.params.distribution STR                           │
│       (default: uniform)                                                     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-base-mass.params.asset-cfg options ──────────────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-base-mass.params.asset-cfg.name STR                         │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.add-base-mass.params.asset-cfg.joint-names {None}|STR|(STR,...) │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.add-base-mass.params.asset-cfg.joint-ids {fixed}                │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.body-names {None}|STR|(STR,...)  │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       base_Link)                                                             │
│ --env.events.add-base-mass.params.asset-cfg.body-ids {fixed}                 │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.geom-names {None}|STR|(STR,...)  │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.add-base-mass.params.asset-cfg.geom-ids {fixed}                 │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.site-names {None}|STR|(STR,...)  │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.add-base-mass.params.asset-cfg.site-ids {fixed}                 │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.actuator-names {None}|STR|[STR,  │
│ ...]                                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.add-base-mass.params.asset-cfg.actuator-ids {fixed}             │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.tendon-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.add-base-mass.params.asset-cfg.tendon-ids {fixed}               │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.camera-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.add-base-mass.params.asset-cfg.camera-ids {fixed}               │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.light-names {None}|STR|(STR,...) │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.add-base-mass.params.asset-cfg.light-ids {fixed}                │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.material-names {None}|STR|(STR,  │
│ ...)                                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.add-base-mass.params.asset-cfg.material-ids {fixed}             │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.add-base-mass.params.asset-cfg.preserve-order {True,False}      │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-link-mass options ───────────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-link-mass.func {fixed}                                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function body_mass at 0x72adc3d78900>)                            │
│ --env.events.add-link-mass.mode {startup,reset,interval,step}                │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: startup)                                  │
│ --env.events.add-link-mass.interval-range-s {None}|(FLOAT,FLOAT)             │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.add-link-mass.is-global-time {True,False}                       │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.add-link-mass.min-step-count-between-reset INT                  │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-link-mass.params options ────────────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-link-mass.params.ranges (FLOAT,...)                         │
│       (default: '(0.8, 1.2)')                                                │
│ --env.events.add-link-mass.params.operation STR                              │
│       (default: scale)                                                       │
│ --env.events.add-link-mass.params.distribution STR                           │
│       (default: uniform)                                                     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.add-link-mass.params.asset-cfg options ──────────────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.add-link-mass.params.asset-cfg.name STR                         │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.add-link-mass.params.asset-cfg.joint-names {None}|STR|(STR,...) │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.add-link-mass.params.asset-cfg.joint-ids {fixed}                │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.body-names {None}|STR|(STR,...)  │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       '.*_[LR]_Link')                                                        │
│ --env.events.add-link-mass.params.asset-cfg.body-ids {fixed}                 │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.geom-names {None}|STR|(STR,...)  │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.add-link-mass.params.asset-cfg.geom-ids {fixed}                 │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.site-names {None}|STR|(STR,...)  │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.add-link-mass.params.asset-cfg.site-ids {fixed}                 │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.actuator-names {None}|STR|[STR,  │
│ ...]                                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.add-link-mass.params.asset-cfg.actuator-ids {fixed}             │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.tendon-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.add-link-mass.params.asset-cfg.tendon-ids {fixed}               │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.camera-names {None}|STR|(STR,    │
│ ...)                                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.add-link-mass.params.asset-cfg.camera-ids {fixed}               │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.light-names {None}|STR|(STR,...) │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.add-link-mass.params.asset-cfg.light-ids {fixed}                │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.material-names {None}|STR|(STR,  │
│ ...)                                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.add-link-mass.params.asset-cfg.material-ids {fixed}             │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.add-link-mass.params.asset-cfg.preserve-order {True,False}      │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-physics-material options ──────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-physics-material.func {fixed}                             │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function geom_friction at 0x72adc3d78360>)                        │
│ --env.events.robot-physics-material.mode {startup,reset,interval,step}       │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: startup)                                  │
│ --env.events.robot-physics-material.interval-range-s {None}|(FLOAT,FLOAT)    │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.is-global-time {True,False}              │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.robot-physics-material.min-step-count-between-reset INT         │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-physics-material.params options ───────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-physics-material.params.operation STR                     │
│       (default: abs)                                                         │
│ --env.events.robot-physics-material.params.distribution STR                  │
│       (default: uniform)                                                     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-physics-material.params.ranges options ────────────────────╮
│ --env.events.robot-physics-material.params.ranges.0 (FLOAT,...)              │
│       (default: '(0.4, 1.2)')                                                │
│ --env.events.robot-physics-material.params.ranges.1 (FLOAT,...)              │
│       (default: '(0.2, 0.9)')                                                │
│ --env.events.robot-physics-material.params.ranges.2 (FLOAT,...)              │
│       (default: '(0.0, 1.0)')                                                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-physics-material.params.asset-cfg options ─────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-physics-material.params.asset-cfg.name STR                │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.robot-physics-material.params.asset-cfg.joint-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.robot-physics-material.params.asset-cfg.joint-ids {fixed}       │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.body-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       '.*')                                                                  │
│ --env.events.robot-physics-material.params.asset-cfg.body-ids {fixed}        │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.geom-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.params.asset-cfg.geom-ids {fixed}        │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.site-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.robot-physics-material.params.asset-cfg.site-ids {fixed}        │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.actuator-names          │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.params.asset-cfg.actuator-ids {fixed}    │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.tendon-names            │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.params.asset-cfg.tendon-ids {fixed}      │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.camera-names            │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.params.asset-cfg.camera-ids {fixed}      │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.light-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.robot-physics-material.params.asset-cfg.light-ids {fixed}       │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.material-names          │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.robot-physics-material.params.asset-cfg.material-ids {fixed}    │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.robot-physics-material.params.asset-cfg.preserve-order {True,   │
│ False}                                                                       │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-center-of-mass options ────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-center-of-mass.func {fixed}                               │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function body_com_offset at 0x72adc3d789a0>)                      │
│ --env.events.robot-center-of-mass.mode {startup,reset,interval,step}         │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: startup)                                  │
│ --env.events.robot-center-of-mass.interval-range-s {None}|(FLOAT,FLOAT)      │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.is-global-time {True,False}                │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.robot-center-of-mass.min-step-count-between-reset INT           │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-center-of-mass.params options ─────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-center-of-mass.params.operation STR                       │
│       (default: add)                                                         │
│ --env.events.robot-center-of-mass.params.distribution STR                    │
│       (default: uniform)                                                     │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-center-of-mass.params.ranges options ──────────────────────╮
│ --env.events.robot-center-of-mass.params.ranges.0 (FLOAT,...)                │
│       (default: '(-0.075, 0.075)')                                           │
│ --env.events.robot-center-of-mass.params.ranges.1 (FLOAT,...)                │
│       (default: '(-0.075, 0.075)')                                           │
│ --env.events.robot-center-of-mass.params.ranges.2 (FLOAT,...)                │
│       (default: '(-0.075, 0.075)')                                           │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.robot-center-of-mass.params.asset-cfg options ───────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.robot-center-of-mass.params.asset-cfg.name STR                  │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.robot-center-of-mass.params.asset-cfg.joint-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.robot-center-of-mass.params.asset-cfg.joint-ids {fixed}         │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.body-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.robot-center-of-mass.params.asset-cfg.body-ids {fixed}          │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.geom-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.params.asset-cfg.geom-ids {fixed}          │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.site-names                │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.robot-center-of-mass.params.asset-cfg.site-ids {fixed}          │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.actuator-names            │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.params.asset-cfg.actuator-ids {fixed}      │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.tendon-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.params.asset-cfg.tendon-ids {fixed}        │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.camera-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.params.asset-cfg.camera-ids {fixed}        │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.light-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.robot-center-of-mass.params.asset-cfg.light-ids {fixed}         │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.material-names            │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.robot-center-of-mass.params.asset-cfg.material-ids {fixed}      │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.robot-center-of-mass.params.asset-cfg.preserve-order {True,     │
│ False}                                                                       │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-robot-base options ────────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.reset-robot-base.func {fixed}                                   │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function reset_root_state_uniform at 0x72adc3d8dd00>)             │
│ --env.events.reset-robot-base.mode {startup,reset,interval,step}             │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: reset)                                    │
│ --env.events.reset-robot-base.interval-range-s {None}|(FLOAT,FLOAT)          │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.reset-robot-base.is-global-time {True,False}                    │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.reset-robot-base.min-step-count-between-reset INT               │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-robot-base.params.pose-range options ──────────────────────╮
│ --env.events.reset-robot-base.params.pose-range.x (FLOAT,...)                │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.pose-range.y (FLOAT,...)                │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.pose-range.yaw (FLOAT,...)              │
│       (default: '(-3.14, 3.14)')                                             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-robot-base.params.velocity-range options ──────────────────╮
│ --env.events.reset-robot-base.params.velocity-range.x (FLOAT,...)            │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.velocity-range.y (FLOAT,...)            │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.velocity-range.z (FLOAT,...)            │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.velocity-range.roll (FLOAT,...)         │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.velocity-range.pitch (FLOAT,...)        │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.reset-robot-base.params.velocity-range.yaw (FLOAT,...)          │
│       (default: '(-0.5, 0.5)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-robot-joints options ──────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.reset-robot-joints.func {fixed}                                 │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function reset_joints_by_offset at 0x72adc3d8de40>)               │
│ --env.events.reset-robot-joints.mode {startup,reset,interval,step}           │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: reset)                                    │
│ --env.events.reset-robot-joints.interval-range-s {None}|(FLOAT,FLOAT)        │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.reset-robot-joints.is-global-time {True,False}                  │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.reset-robot-joints.min-step-count-between-reset INT             │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.reset-robot-joints.params options ───────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.reset-robot-joints.params.position-range (FLOAT,...)            │
│       (default: '(-0.2, 0.2)')                                               │
│ --env.events.reset-robot-joints.params.velocity-range (FLOAT,...)            │
│       (default: '(-0.5, 0.5)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-stiffness options ───────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-stiffness.func {fixed}                          │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_stiffness at 0x72adc3d78f40>)                      │
│ --env.events.randomize-joint-stiffness.mode {startup,reset,interval,step}    │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: reset)                                    │
│ --env.events.randomize-joint-stiffness.interval-range-s {None}|(FLOAT,FLOAT) │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.is-global-time {True,False}           │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.randomize-joint-stiffness.min-step-count-between-reset INT      │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-stiffness.params options ────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-stiffness.params.ranges (FLOAT,...)             │
│       (default: '(0.5, 2.0)')                                                │
│ --env.events.randomize-joint-stiffness.params.operation STR                  │
│       (default: scale)                                                       │
│ --env.events.randomize-joint-stiffness.params.distribution STR               │
│       (default: log_uniform)                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-stiffness.params.asset-cfg options ──────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.name STR             │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.joint-names          │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '.*')                                                                  │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.joint-ids {fixed}    │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.body-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.body-ids {fixed}     │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.geom-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.geom-ids {fixed}     │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.site-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.site-ids {fixed}     │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.actuator-names       │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.actuator-ids {fixed} │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.tendon-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.tendon-ids {fixed}   │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.camera-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.camera-ids {fixed}   │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.light-names          │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.light-ids {fixed}    │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.material-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.material-ids {fixed} │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-stiffness.params.asset-cfg.preserve-order       │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-damping options ─────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-damping.func {fixed}                            │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function joint_damping at 0x72adc3d78d60>)                        │
│ --env.events.randomize-joint-damping.mode {startup,reset,interval,step}      │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: reset)                                    │
│ --env.events.randomize-joint-damping.interval-range-s {None}|(FLOAT,FLOAT)   │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.is-global-time {True,False}             │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.randomize-joint-damping.min-step-count-between-reset INT        │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-damping.params options ──────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-damping.params.ranges (FLOAT,...)               │
│       (default: '(0.5, 2.0)')                                                │
│ --env.events.randomize-joint-damping.params.operation STR                    │
│       (default: scale)                                                       │
│ --env.events.randomize-joint-damping.params.distribution STR                 │
│       (default: log_uniform)                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.randomize-joint-damping.params.asset-cfg options ────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.randomize-joint-damping.params.asset-cfg.name STR               │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.events.randomize-joint-damping.params.asset-cfg.joint-names            │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '.*')                                                                  │
│ --env.events.randomize-joint-damping.params.asset-cfg.joint-ids {fixed}      │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.body-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.randomize-joint-damping.params.asset-cfg.body-ids {fixed}       │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.geom-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.params.asset-cfg.geom-ids {fixed}       │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.site-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.events.randomize-joint-damping.params.asset-cfg.site-ids {fixed}       │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.actuator-names         │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.params.asset-cfg.actuator-ids {fixed}   │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.tendon-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.params.asset-cfg.tendon-ids {fixed}     │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.camera-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.params.asset-cfg.camera-ids {fixed}     │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.light-names            │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.events.randomize-joint-damping.params.asset-cfg.light-ids {fixed}      │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.material-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.events.randomize-joint-damping.params.asset-cfg.material-ids {fixed}   │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.events.randomize-joint-damping.params.asset-cfg.preserve-order {True,  │
│ False}                                                                       │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.push-robot options ──────────────────────────────────────────────╮
│ Configuration for an event term. Event terms trigger operations at specific  │
│ simulation events. They're commonly used for domain randomization, state     │
│ resets, and periodic perturbations.                                          │
│                                                                              │
│ The four modes determine when the event fires:                               │
│                                                                              │
│                                                                              │
│ - ``"startup"``: Once when the environment initializes. Use for parameters   │
│ that                                                                         │
│   should be randomized per-environment but stay constant within an episode   │
│ (e.g.,                                                                       │
│   domain randomization).                                                     │
│                                                                              │
│                                                                              │
│ - ``"reset"``: On every episode reset. Use for parameters that should vary   │
│ between                                                                      │
│   episodes (e.g., initial robot pose, domain randomization).                 │
│                                                                              │
│                                                                              │
│ - ``"interval"``: Periodically during simulation, controlled by              │
│ ``interval_range_s``.                                                        │
│   Use for perturbations that should happen during episodes (e.g., pushing    │
│ the robot,                                                                   │
│   external disturbances).                                                    │
│                                                                              │
│                                                                              │
│ - ``"step"``: Every environment step, unconditionally on all envs. Use for   │
│ terms that                                                                   │
│   manage per-step state such as force lifetimes (e.g.,                       │
│ ``apply_body_impulse``).                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.events.push-robot.func {fixed}                                         │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function push_by_setting_velocity at 0x72adc3d8dee0>)             │
│ --env.events.push-robot.mode {startup,reset,interval,step}                   │
│       When the event triggers: ``"startup"`` (once at init), ``"reset"``     │
│       (every                                                                 │
│       episode), ``"interval"`` (periodically during simulation), or          │
│       ``"step"`` (every                                                      │
│       environment step). (default: interval)                                 │
│ --env.events.push-robot.interval-range-s {None}|(FLOAT,FLOAT)                │
│       Time range in seconds for interval mode. The next trigger time is      │
│       uniformly                                                              │
│       sampled from ``[min, max]``. Required when ``mode="interval"``.        │
│       (default: '(1.0, 3.0)')                                                │
│ --env.events.push-robot.is-global-time {True,False}                          │
│       Whether all environments share the same timer. If True, all envs       │
│       trigger                                                                │
│       simultaneously. If False (default), each env has an independent timer  │
│       that                                                                   │
│       resets on episode reset. Only applies to ``mode="interval"``.          │
│       (default: False)                                                       │
│ --env.events.push-robot.min-step-count-between-reset INT                     │
│       Minimum environment steps between triggers. Prevents the event from    │
│       firing                                                                 │
│       too frequently when episodes reset rapidly. Only applies to            │
│       ``mode="reset"``.                                                      │
│       Set to 0 (default) to trigger on every reset. (default: 0)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.events.push-robot.params.velocity-range options ────────────────────────╮
│ --env.events.push-robot.params.velocity-range.x (FLOAT,...)                  │
│       (default: '(-0.5, 0.5)')                                               │
│ --env.events.push-robot.params.velocity-range.y (FLOAT,...)                  │
│       (default: '(-0.5, 0.5)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim options ────────────────────────────────────────────────────────────╮
│ Simulation configuration including physics timestep, solver iterations,      │
│ contact parameters, and NaN guarding.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.sim.nconmax {None}|INT                                                 │
│       Number of contacts to allocate per world.                              │
│                                                                              │
│                                                                              │
│       Contacts exist in large heterogenous arrays: one world may have more   │
│       than nconmax                                                           │
│       contacts. If None, a heuristic value is used. (default: 256)           │
│ --env.sim.njmax {None}|INT                                                   │
│       Number of constraints to allocate per world.                           │
│                                                                              │
│                                                                              │
│       Constraint arrays are batched by world: no world may have more than    │
│       njmax                                                                  │
│       constraints. If None, a heuristic value is used. (default: 512)        │
│ --env.sim.ls-parallel {True,False}                                           │
│       Boosts perf quite noticeably. (default: True)                          │
│ --env.sim.contact-sensor-maxmatch INT                                        │
│       (default: 64)                                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim.mujoco options ─────────────────────────────────────────────────────╮
│ --env.sim.mujoco.timestep FLOAT                                              │
│       Integrator settings. (default: 0.005)                                  │
│ --env.sim.mujoco.integrator {euler,implicitfast}                             │
│       Integrator settings. (default: implicitfast)                           │
│ --env.sim.mujoco.impratio FLOAT                                              │
│       Friction settings. (default: 1.0)                                      │
│ --env.sim.mujoco.cone {pyramidal,elliptic}                                   │
│       Friction settings. (default: pyramidal)                                │
│ --env.sim.mujoco.jacobian {auto,dense,sparse}                                │
│       Solver settings. (default: auto)                                       │
│ --env.sim.mujoco.solver {newton,cg,pgs}                                      │
│       Solver settings. (default: newton)                                     │
│ --env.sim.mujoco.iterations INT                                              │
│       Solver settings. (default: 10)                                         │
│ --env.sim.mujoco.tolerance FLOAT                                             │
│       Solver settings. (default: 1e-08)                                      │
│ --env.sim.mujoco.ls-iterations INT                                           │
│       Solver settings. (default: 20)                                         │
│ --env.sim.mujoco.ls-tolerance FLOAT                                          │
│       Solver settings. (default: 0.01)                                       │
│ --env.sim.mujoco.ccd-iterations INT                                          │
│       Solver settings. (default: 50)                                         │
│ --env.sim.mujoco.gravity (FLOAT,FLOAT,FLOAT)                                 │
│       Other. (default: '(0.0, 0.0, -9.81)')                                  │
│ --env.sim.mujoco.multiccd {True,False}                                       │
│       Other. (default: False)                                                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.sim.nan-guard options ──────────────────────────────────────────────────╮
│ --env.sim.nan-guard.enabled {True,False}                                     │
│       (default: False)                                                       │
│ --env.sim.nan-guard.buffer-size INT                                          │
│       (default: 100)                                                         │
│ --env.sim.nan-guard.output-dir STR                                           │
│       (default: /tmp/mjlab/nan_dumps)                                        │
│ --env.sim.nan-guard.max-envs-to-dump INT                                     │
│       (default: 5)                                                           │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.viewer options ─────────────────────────────────────────────────────────╮
│ Viewer configuration for rendering (camera position, resolution, etc.).      │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.viewer.lookat (FLOAT,FLOAT,FLOAT)                                      │
│       (default: '(0.0, 0.0, 0.0)')                                           │
│ --env.viewer.distance FLOAT                                                  │
│       (default: 3.0)                                                         │
│ --env.viewer.fovy {None}|FLOAT                                               │
│       (default: None)                                                        │
│ --env.viewer.elevation FLOAT                                                 │
│       (default: 10.0)                                                        │
│ --env.viewer.azimuth FLOAT                                                   │
│       (default: 90.0)                                                        │
│ --env.viewer.origin-type {AUTO,WORLD,ASSET_ROOT,ASSET_BODY}                  │
│       (default: ASSET_BODY)                                                  │
│ --env.viewer.entity-name {None}|STR                                          │
│       (default: robot)                                                       │
│ --env.viewer.body-name {None}|STR                                            │
│       (default: base_Link)                                                   │
│ --env.viewer.env-idx INT                                                     │
│       (default: 0)                                                           │
│ --env.viewer.max-extra-envs INT                                              │
│       Number of neighboring environments to render around ``env_idx``.       │
│       (default: 2)                                                           │
│ --env.viewer.enable-reflections {True,False}                                 │
│       (default: True)                                                        │
│ --env.viewer.enable-shadows {True,False}                                     │
│       (default: True)                                                        │
│ --env.viewer.height INT                                                      │
│       (default: 240)                                                         │
│ --env.viewer.width INT                                                       │
│       (default: 320)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.safety-exp options ─────────────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.safety-exp.func {fixed}                                        │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function safety_reward_exp at 0x72adbe5c1f80>)                        │
│ --env.rewards.safety-exp.weight FLOAT                                        │
│       Weight multiplier for this reward term. (default: 1.0)                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.safety-exp.params options ──────────────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.safety-exp.params.base-height-target FLOAT                     │
│       (default: 0.9)                                                         │
│ --env.rewards.safety-exp.params.std FLOAT                                    │
│       (default: 0.7071067811865476)                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-position-exp options ────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-position-exp.func {fixed}                           │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function track_base_position_exp at 0x72adbe5c2020>)                  │
│ --env.rewards.track-base-position-exp.weight FLOAT                           │
│       Weight multiplier for this reward term. (default: 10.0)                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-position-exp.params options ─────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-position-exp.params.command-name STR                │
│       (default: base_pose)                                                   │
│ --env.rewards.track-base-position-exp.params.std FLOAT                       │
│       (default: 0.7071067811865476)                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-orientation-exp options ─────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-orientation-exp.func {fixed}                        │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function track_base_orientation_exp at 0x72adbe5c20c0>)               │
│ --env.rewards.track-base-orientation-exp.weight FLOAT                        │
│       Weight multiplier for this reward term. (default: 0.0)                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-orientation-exp.params options ──────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-orientation-exp.params.command-name STR             │
│       (default: base_pose)                                                   │
│ --env.rewards.track-base-orientation-exp.params.std FLOAT                    │
│       (default: 0.7071067811865476)                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-pb options ──────────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-pb.func {fixed}                                     │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function track_base_pb at 0x72adbe5c2160>)                            │
│ --env.rewards.track-base-pb.weight FLOAT                                     │
│       Weight multiplier for this reward term. (default: 0.0)                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-reference-exp options ───────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-reference-exp.func {fixed}                          │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function track_base_reference_exp at 0x72adbe5c2200>)                 │
│ --env.rewards.track-base-reference-exp.weight FLOAT                          │
│       Weight multiplier for this reward term. (default: 0.0)                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.track-base-reference-exp.params options ────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.track-base-reference-exp.params.std FLOAT                      │
│       (default: 0.7071067811865476)                                          │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-weighted-torques-l2 options ────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-weighted-torques-l2.func {fixed}                           │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function weighted_joint_torques_l2 at 0x72adbe5c2340>)                │
│ --env.rewards.dof-weighted-torques-l2.weight FLOAT                           │
│       Weight multiplier for this reward term. (default: -4e-05)              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-weighted-torques-l2.params.torque-weight options ───────────╮
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.abad-L-Joint      │
│ FLOAT                                                                        │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.hip-L-Joint FLOAT │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.knee-L-Joint      │
│ FLOAT                                                                        │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.abad-R-Joint      │
│ FLOAT                                                                        │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.hip-R-Joint FLOAT │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.knee-R-Joint      │
│ FLOAT                                                                        │
│       (default: 0.2)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.wheel-L-Joint     │
│ FLOAT                                                                        │
│       (default: 8.0)                                                         │
│ --env.rewards.dof-weighted-torques-l2.params.torque-weight.wheel-R-Joint     │
│ FLOAT                                                                        │
│       (default: 8.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-weighted-power-l1 options ──────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-weighted-power-l1.func {fixed}                             │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function weighted_joint_power_l1 at 0x72adbe5c23e0>)                  │
│ --env.rewards.dof-weighted-power-l1.weight FLOAT                             │
│       Weight multiplier for this reward term. (default: -0.00025)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-weighted-power-l1.params.power-weight options ──────────────╮
│ --env.rewards.dof-weighted-power-l1.params.power-weight.abad-L-Joint FLOAT   │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.hip-L-Joint FLOAT    │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.knee-L-Joint FLOAT   │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.abad-R-Joint FLOAT   │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.hip-R-Joint FLOAT    │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.knee-R-Joint FLOAT   │
│       (default: 1.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.wheel-L-Joint FLOAT  │
│       (default: 2.0)                                                         │
│ --env.rewards.dof-weighted-power-l1.params.power-weight.wheel-R-Joint FLOAT  │
│       (default: 2.0)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.action-rate-l2 options ─────────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.action-rate-l2.func {fixed}                                    │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function action_rate_l2 at 0x72adc3d8f380>)                           │
│ --env.rewards.action-rate-l2.weight FLOAT                                    │
│       Weight multiplier for this reward term. (default: -0.002)              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.action-smoothness options ──────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.action-smoothness.func {fixed}                                 │
│       The callable that computes this reward term's value. (fixed to:        │
│       <tron1_rl_mjlab.tasks.mdp.rewards.ActionSmoothnessPenaltyWrapper       │
│       object at 0x72adbe5d9590>)                                             │
│ --env.rewards.action-smoothness.weight FLOAT                                 │
│       Weight multiplier for this reward term. (default: -0.006)              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-vel-wheel-l2 options ───────────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-vel-wheel-l2.func {fixed}                                  │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function joint_vel_l2 at 0x72adbe5c22a0>)                             │
│ --env.rewards.dof-vel-wheel-l2.weight FLOAT                                  │
│       Weight multiplier for this reward term. (default: -0.0005)             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-vel-wheel-l2.params.asset-cfg options ──────────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.name STR                     │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.joint-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       wheel_.+)                                                              │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.joint-ids {fixed}            │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.body-names {None}|STR|(STR,  │
│ ...)                                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.body-ids {fixed}             │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.geom-names {None}|STR|(STR,  │
│ ...)                                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.geom-ids {fixed}             │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.site-names {None}|STR|(STR,  │
│ ...)                                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.site-ids {fixed}             │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.actuator-names               │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.actuator-ids {fixed}         │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.tendon-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.tendon-ids {fixed}           │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.camera-names                 │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.camera-ids {fixed}           │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.light-names {None}|STR|(STR, │
│ ...)                                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.light-ids {fixed}            │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.material-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.material-ids {fixed}         │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-wheel-l2.params.asset-cfg.preserve-order {True,False}  │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-vel-non-wheel-l2 options ───────────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-vel-non-wheel-l2.func {fixed}                              │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function joint_vel_l2 at 0x72adbe5c22a0>)                             │
│ --env.rewards.dof-vel-non-wheel-l2.weight FLOAT                              │
│       Weight multiplier for this reward term. (default: -0.001)              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-vel-non-wheel-l2.params.asset-cfg options ──────────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.name STR                 │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.joint-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '(?!wheel_).*')                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.joint-ids {fixed}        │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.body-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.body-ids {fixed}         │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.geom-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.geom-ids {fixed}         │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.site-names               │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.site-ids {fixed}         │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.actuator-names           │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.actuator-ids {fixed}     │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.tendon-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.tendon-ids {fixed}       │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.camera-names             │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.camera-ids {fixed}       │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.light-names              │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.light-ids {fixed}        │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.material-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.material-ids {fixed}     │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-vel-non-wheel-l2.params.asset-cfg.preserve-order {True,    │
│ False}                                                                       │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-non-wheel-pos-limits options ───────────────────────────────╮
│ Configuration for a reward term.                                             │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-non-wheel-pos-limits.func {fixed}                          │
│       The callable that computes this reward term's value. (fixed to:        │
│       <function joint_pos_limits at 0x72adc3d8f4c0>)                         │
│ --env.rewards.dof-non-wheel-pos-limits.weight FLOAT                          │
│       Weight multiplier for this reward term. (default: -5.0)                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.rewards.dof-non-wheel-pos-limits.params.asset-cfg options ──────────────╮
│ Configuration for a scene entity that is used by the manager's term. This    │
│ configuration allows flexible specification of entity components either by   │
│ name or by ID. During resolution, it ensures consistency between names and   │
│ IDs, and can optimize to slice(None) when all components are selected.       │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.name STR             │
│       The name of the entity in the scene. (default: robot)                  │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.joint-names          │
│ {None}|STR|(STR,...)                                                         │
│       Names of joints to include. Can be a single string or tuple. (default: │
│       '(?!wheel_).*')                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.joint-ids {fixed}    │
│       IDs of joints to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.body-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of bodies to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.body-ids {fixed}     │
│       IDs of bodies to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.geom-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of geometries to include. Can be a single string or tuple.       │
│       (default: None)                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.geom-ids {fixed}     │
│       IDs of geometries to include. Can be a list or slice. (fixed to:       │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.site-names           │
│ {None}|STR|(STR,...)                                                         │
│       Names of sites to include. Can be a single string or tuple. (default:  │
│       None)                                                                  │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.site-ids {fixed}     │
│       IDs of sites to include. Can be a list or slice. (fixed to:            │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.actuator-names       │
│ {None}|STR|[STR,...]                                                         │
│       Names of actuators to include. Can be a single string or list.         │
│       (default: None)                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.actuator-ids {fixed} │
│       IDs of actuators to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.tendon-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of tendons to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.tendon-ids {fixed}   │
│       IDs of tendons to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.camera-names         │
│ {None}|STR|(STR,...)                                                         │
│       Names of cameras to include. Can be a single string or tuple.          │
│       (default: None)                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.camera-ids {fixed}   │
│       IDs of cameras to include. Can be a list or slice. (fixed to:          │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.light-names          │
│ {None}|STR|(STR,...)                                                         │
│       Names of lights to include. Can be a single string or tuple. (default: │
│       None)                                                                  │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.light-ids {fixed}    │
│       IDs of lights to include. Can be a list or slice. (fixed to:           │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.material-names       │
│ {None}|STR|(STR,...)                                                         │
│       Names of materials to include. Can be a single string or tuple.        │
│       (default: None)                                                        │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.material-ids {fixed} │
│       IDs of materials to include. Can be a list or slice. (fixed to:        │
│       slice(None, None, None))                                               │
│ --env.rewards.dof-non-wheel-pos-limits.params.asset-cfg.preserve-order       │
│ {True,False}                                                                 │
│       If True, maintains the order of components as specified. (default:     │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.time-out options ──────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.time-out.func {fixed}                                     │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function time_out at 0x72adc3d8f920>)                             │
│ --env.terminations.time-out.time-out {True,False}                            │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       True)                                                                  │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.bad-orientation options ───────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.bad-orientation.func {fixed}                              │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function bad_orientation_stochastic at 0x72adbe5c2840>)           │
│ --env.terminations.bad-orientation.time-out {True,False}                     │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.bad-orientation.params options ────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.bad-orientation.params.limit-angle FLOAT                  │
│       (default: 1.2566370614359172)                                          │
│ --env.terminations.bad-orientation.params.probability FLOAT                  │
│       (default: 0.1)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.bad-height options ────────────────────────────────────────╮
│ Configuration for a termination term.                                        │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.bad-height.func {fixed}                                   │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function bad_height_stochastic at 0x72adbe5c28e0>)                │
│ --env.terminations.bad-height.time-out {True,False}                          │
│       Whether the term contributes towards episodic timeouts. (default:      │
│       False)                                                                 │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.terminations.bad-height.params options ─────────────────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.terminations.bad-height.params.limit-height FLOAT                      │
│       (default: 0.5)                                                         │
│ --env.terminations.bad-height.params.probability FLOAT                       │
│       (default: 0.1)                                                         │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.commands.base-pose options ─────────────────────────────────────────────╮
│ --env.commands.base-pose.resampling-time-range (FLOAT,FLOAT)                 │
│       Time range in seconds for command resampling. When the timer expires,  │
│       a new                                                                  │
│       command is sampled and the timer is reset to a value uniformly drawn   │
│       from                                                                   │
│       ``[min, max]``. Set both values equal for fixed-interval resampling.   │
│       (default: '(14.0, 15.0)')                                              │
│ --env.commands.base-pose.debug-vis {True,False}                              │
│       Whether to enable debug visualization for this command term. When      │
│       True,                                                                  │
│       the command term's ``_debug_vis_impl`` method is called each frame to  │
│       render                                                                 │
│       visual aids (e.g., velocity arrows, target markers). (default: True)   │
│ --env.commands.base-pose.entity-name STR                                     │
│       (default: robot)                                                       │
│ --env.commands.base-pose.body-name STR                                       │
│       (default: base_Link)                                                   │
│ --env.commands.base-pose.make-quat-unique {True,False}                       │
│       (default: False)                                                       │
│ --env.commands.base-pose.se3-decrease-vel-range (FLOAT,FLOAT)                │
│       (default: '(0.5, 1.4)')                                                │
│ --env.commands.base-pose.resampling-time-scale (FLOAT,FLOAT)                 │
│       (default: '(0.5, 5.0)')                                                │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.commands.base-pose.ranges options ──────────────────────────────────────╮
│ --env.commands.base-pose.ranges.pos-x (FLOAT,FLOAT)                          │
│       (default: '(-0.2, 0.2)')                                               │
│ --env.commands.base-pose.ranges.pos-y (FLOAT,FLOAT)                          │
│       (default: '(-0.2, 0.2)')                                               │
│ --env.commands.base-pose.ranges.vel-x (FLOAT,FLOAT)                          │
│       (default: '(-0.0, 0.0)')                                               │
│ --env.commands.base-pose.ranges.vel-y (FLOAT,FLOAT)                          │
│       (default: '(-0.0, 0.0)')                                               │
│ --env.commands.base-pose.ranges.vel-yaw (FLOAT,FLOAT)                        │
│       (default: '(-0.0, 0.0)')                                               │
│ --env.commands.base-pose.ranges.pos-z (FLOAT,FLOAT)                          │
│       Fixed values (not configurable) (default: '(0.7, 1.1)')                │
│ --env.commands.base-pose.ranges.roll (FLOAT,FLOAT)                           │
│       Fixed values (not configurable) (default: '(0.0, 0.0)')                │
│ --env.commands.base-pose.ranges.pitch (FLOAT,FLOAT)                          │
│       Fixed values (not configurable) (default: '(0.0, 0.0)')                │
│ --env.commands.base-pose.ranges.yaw (FLOAT,FLOAT)                            │
│       Fixed values (not configurable) (default: '(-3.14, 3.14)')             │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.curriculum.pos-commands-ranges-level options ───────────────────────────╮
│ Configuration for a curriculum term. Curriculum terms modify environment     │
│ parameters during training to implement curriculum learning strategies       │
│ (e.g., gradually increasing task difficulty).                                │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.curriculum.pos-commands-ranges-level.func {fixed}                      │
│       The callable that computes this term's value. Can be a function or a   │
│       class.                                                                 │
│       Classes are auto-instantiated with ``(cfg=term_cfg, env=env)``. (fixed │
│       to: <function pos_commands_ranges_level at 0x72adbe5c2ca0>)            │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.curriculum.pos-commands-ranges-level.params options ────────────────────╮
│ Additional keyword arguments passed to func when called.                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --env.curriculum.pos-commands-ranges-level.params.update-interval INT        │
│       (default: 1920)                                                        │
│ --env.curriculum.pos-commands-ranges-level.params.command-name STR           │
│       (default: base_pose)                                                   │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ env.curriculum.pos-commands-ranges-level.params.max-range options ──────────╮
│ --env.curriculum.pos-commands-ranges-level.params.max-range.pos-x (FLOAT,    │
│ ...)                                                                         │
│       (default: '(-1.0, 1.0)')                                               │
│ --env.curriculum.pos-commands-ranges-level.params.max-range.pos-y (FLOAT,    │
│ ...)                                                                         │
│       (default: '(-1.0, 1.0)')                                               │
│ --env.curriculum.pos-commands-ranges-level.params.max-range.vel-x (FLOAT,    │
│ ...)                                                                         │
│       (default: '(-1.0, 1.0)')                                               │
│ --env.curriculum.pos-commands-ranges-level.params.max-range.vel-y (FLOAT,    │
│ ...)                                                                         │
│       (default: '(-1.0, 1.0)')                                               │
│ --env.curriculum.pos-commands-ranges-level.params.max-range.vel-yaw (FLOAT,  │
│ ...)                                                                         │
│       (default: '(-2.0, 2.0)')                                               │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent options ──────────────────────────────────────────────────────────────╮
│ --agent.seed INT                                                             │
│       The seed for the experiment. Default is 42. (default: 42)              │
│ --agent.num-steps-per-env INT                                                │
│       The number of steps per environment update. (default: 24)              │
│ --agent.max-iterations INT                                                   │
│       The maximum number of iterations. (default: 20000)                     │
│ --agent.save-interval INT                                                    │
│       The number of iterations between saves. (default: 200)                 │
│ --agent.experiment-name STR                                                  │
│       Directory name used to group runs under                                │
│       ``logs/rsl_rl/{experiment_name}/``. (default: wf_tron)                 │
│ --agent.run-name STR                                                         │
│       Optional label appended to the timestamped run directory               │
│       (e.g. ``2025-01-27_14-30-00_{run_name}``). Also becomes the            │
│       display name for the run in wandb. (default: '')                       │
│ --agent.logger {wandb,tensorboard}                                           │
│       The logger to use. Default is wandb. (default: wandb)                  │
│ --agent.wandb-project STR                                                    │
│       The wandb project name. (default: mjlab_wf_tron)                       │
│ --agent.wandb-tags (STR,...)                                                 │
│       Tags for the wandb run. Default is empty tuple. (default: '()')        │
│ --agent.resume {True,False}                                                  │
│       Whether to resume the experiment. Default is False. (default: False)   │
│ --agent.load-run STR                                                         │
│       The run directory to load. Default is ".*" which means all runs. If    │
│       regex                                                                  │
│       expression, the latest (alphabetical order) matching run will be       │
│       loaded. (default: '.*')                                                │
│ --agent.load-checkpoint STR                                                  │
│       The checkpoint file to load. Default is "model_.*.pt" (all). If regex  │
│       expression,                                                            │
│       the latest (alphabetical order) matching file will be loaded.          │
│       (default: 'model_.*.pt')                                               │
│ --agent.clip-actions {None}|FLOAT                                            │
│       The clipping range for action values. If None (default), no clipping   │
│       is applied. (default: None)                                            │
│ --agent.upload-model {True,False}                                            │
│       Whether to upload model files (.pt, .onnx) to W&B on save. Set to      │
│       False to keep metric logging but avoid storage usage. Default is True. │
│       (default: True)                                                        │
│ --agent.class-name STR                                                       │
│       The runner class name. Default is OnPolicyRunner. (default:            │
│       OnPolicyRunner)                                                        │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.obs-groups options ───────────────────────────────────────────────────╮
│ --agent.obs-groups.actor (STR,...)                                           │
│       (default: '('"'"'actor'"'"',)')                                        │
│ --agent.obs-groups.critic (STR,...)                                          │
│       (default: '('"'"'critic'"'"',)')                                       │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.actor options ────────────────────────────────────────────────────────╮
│ The actor configuration.                                                     │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.actor.hidden-dims (INT,...)                                          │
│       The hidden dimensions of the network. (default: '(512, 256, 128)')     │
│ --agent.actor.activation STR                                                 │
│       The activation function. (default: elu)                                │
│ --agent.actor.obs-normalization {True,False}                                 │
│       Whether to normalize the observations. Default is False. (default:     │
│       False)                                                                 │
│ --agent.actor.class-name STR                                                 │
│       Model class name resolved by RSL-RL (MLPModel or CNNModel). (default:  │
│       MLPModel)                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.actor.distribution-cfg options ───────────────────────────────────────╮
│ --agent.actor.distribution-cfg.class-name STR                                │
│       (default: GaussianDistribution)                                        │
│ --agent.actor.distribution-cfg.init-std FLOAT                                │
│       (default: 1.0)                                                         │
│ --agent.actor.distribution-cfg.std-type STR                                  │
│       (default: scalar)                                                      │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.critic options ───────────────────────────────────────────────────────╮
│ The critic configuration.                                                    │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.critic.hidden-dims (INT,...)                                         │
│       The hidden dimensions of the network. (default: '(512, 256, 128)')     │
│ --agent.critic.activation STR                                                │
│       The activation function. (default: elu)                                │
│ --agent.critic.obs-normalization {True,False}                                │
│       Whether to normalize the observations. Default is False. (default:     │
│       False)                                                                 │
│ --agent.critic.class-name STR                                                │
│       Model class name resolved by RSL-RL (MLPModel or CNNModel). (default:  │
│       MLPModel)                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯
╭─ agent.algorithm options ────────────────────────────────────────────────────╮
│ The algorithm configuration.                                                 │
│ ──────────────────────────────────────────────────────────────────────────── │
│ --agent.algorithm.num-learning-epochs INT                                    │
│       The number of learning epochs per update. (default: 5)                 │
│ --agent.algorithm.num-mini-batches INT                                       │
│       The number of mini-batches per update.                                 │
│       mini batch size = num_envs * num_steps / num_mini_batches (default: 4) │
│ --agent.algorithm.learning-rate FLOAT                                        │
│       The learning rate. (default: 0.001)                                    │
│ --agent.algorithm.schedule {adaptive,fixed}                                  │
│       The learning rate schedule. (default: adaptive)                        │
│ --agent.algorithm.gamma FLOAT                                                │
│       The discount factor. (default: 0.99)                                   │
│ --agent.algorithm.lam FLOAT                                                  │
│       The lambda parameter for Generalized Advantage Estimation (GAE).       │
│       (default: 0.95)                                                        │
│ --agent.algorithm.entropy-coef FLOAT                                         │
│       The coefficient for the entropy loss. (default: 0.01)                  │
│ --agent.algorithm.desired-kl FLOAT                                           │
│       The desired KL divergence between the new and old policies. (default:  │
│       0.01)                                                                  │
│ --agent.algorithm.max-grad-norm FLOAT                                        │
│       The maximum gradient norm for the policy. (default: 1.0)               │
│ --agent.algorithm.value-loss-coef FLOAT                                      │
│       The coefficient for the value loss. (default: 1.0)                     │
│ --agent.algorithm.use-clipped-value-loss {True,False}                        │
│       Whether to use clipped value loss. (default: True)                     │
│ --agent.algorithm.clip-param FLOAT                                           │
│       The clipping parameter for the policy. (default: 0.2)                  │
│ --agent.algorithm.normalize-advantage-per-mini-batch {True,False}            │
│       Whether to normalize the advantage per mini-batch. Default is False.   │
│       If True, the                                                           │
│       advantage is normalized over the mini-batches only. Otherwise, the     │
│       advantage is                                                           │
│       normalized over the entire collected trajectories. (default: False)    │
│ --agent.algorithm.optimizer {adam,adamw,sgd,rmsprop}                         │
│       The optimizer to use. (default: adam)                                  │
│ --agent.algorithm.share-cnn-encoders {True,False}                            │
│       Share CNN encoders between actor and critic. (default: False)          │
│ --agent.algorithm.class-name STR                                             │
│       Algorithm class name resolved by RSL-RL. (default: PPO)                │
╰──────────────────────────────────────────────────────────────────────────────╯
